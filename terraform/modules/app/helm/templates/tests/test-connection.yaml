apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shortlet-current-time.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "shortlet-current-time.name" . }}
    helm.sh/chart: {{ include "shortlet-current-time.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "shortlet-current-time.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
